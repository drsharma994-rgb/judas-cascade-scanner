{
  "name": "judas-cascade-scanner",
  "version": "4.0.0",
  "private": true,
  "description": "Delta Scanner v4 — strict 8-gate Judas Cascade PWA with a Node/Express backend serving live Delta Exchange India public market data.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check scanner.js && node --check app.js && node --check tradelog.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
